name: obs_mcp_js
description: OBS Studio MCP server compiled to JavaScript for Node.js execution via npx.
version: 5.7.5
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  obs_websocket: ^5.7.0+3
  dart_mcp: ^0.5.1
  stream_channel: ^2.1.4
  web_socket_channel: ^3.0.2
  web: ^1.1.0
  loggy: ^2.0.3
